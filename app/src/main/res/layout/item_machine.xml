<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:padding="30dp"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <ImageView
        android:id="@+id/imgSketchMachine"
        android:layout_width="100dp"
        android:layout_height="70dp"
        tools:src="@drawable/ic_22"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        />

    <TextView
        android:id="@+id/tvNameMachine"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="30sp"
        app:layout_constraintEnd_toEndOf="parent"
        tools:text="22"
        app:layout_constraintStart_toEndOf="@+id/imgSketchMachine"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/tvCountMachine"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        tools:text="15шт"
        app:layout_constraintStart_toEndOf="@+id/imgSketchMachine"
        app:layout_constraintTop_toBottomOf="@+id/tvNameMachine" />

</androidx.constraintlayout.widget.ConstraintLayout>